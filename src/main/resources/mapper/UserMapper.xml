<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <resultMap id="userResultMap" type="com.example.demo.domain.Users">
        <constructor>
            <idArg  column="id" javaType="int"></idArg>
        </constructor>
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="email" column="email"/>

    </resultMap>
    <insert id="insertUser"  parameterType="com.example.demo.domain.Users">
        INSERT  INTO userinfo (id,name,password,email) value (#{id},#{name},#{password},#{email})
    </insert>
    <insert id="batchInsert">
        INSERT  INTO userinfo (id,name,password,email) values
        <foreach collection="list" item="item" separator=",">(#{item.id},#{item.name},#{item.password},#{item.email})</foreach>
    </insert>
    <select id="queryInfo" parameterType="java.lang.String" resultType="com.example.demo.domain.Users">
        select id,name,password,email from userinfo where name = #{name}
    </select>
    <update id="updateUser">
        update  Users set name=#{name} ,email = #{email} where id=#{id}
    </update>
    <select id="queryInfoById" resultType="com.example.demo.domain.Users">
        select * from userinfo where  id = #{id};
    </select>
    <select id="getMutiUser" resultType="org.apache.catalina.User" parameterType="java.util.Map">
        select * from userinfo where id=#{id};
    </select>
    <select id="queryUser" resultType="com.example.demo.domain.Users" parameterType="map">
        select * from users <where>
        <if test="''!=name and null !=name">
            name=#{name}
        </if>
        <if test="''!=email and null !=email">
            and  email=#{email}
        </if>
        <if test="''!=id and null!=id">
            and id=#{id}
        </if>
        </where>
    </select>
    <select id="getUser" resultType="com.example.demo.domain.Users">
        select id,name,email from users where id=#{id}
    </select>
    <delete id="deleteUser">
        delete from userinfo where id = #{id};
    </delete>
</mapper>